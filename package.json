{
  "name": "aset",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "start": "pnpm bundle && node ./dist/index.js",
    "bundle": "rm -rf dist && esbuild --bundle --format=esm --outdir=dist src/index.ts --platform=node",
    "lint": "tsc --noEmit --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.8",
    "esbuild": "^0.15.12",
    "typescript": "^4.8.4"
  }
}
